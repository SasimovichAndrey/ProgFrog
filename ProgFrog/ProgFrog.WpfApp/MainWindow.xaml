<Window x:Class="ProgFrog.WpfApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProgFrog.WpfApp"
        mc:Ignorable="d"
        Title="MainWindow" Height="550" Width="825">
    <Window.Resources>
        <DataTemplate x:Key="TaskListItemTemplate">
            <TextBlock Text="{Binding Path=Identifier.StringPresentation}"
                        Foreground="Navy" FontWeight="Bold" FontSize="12"/>
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListBox SelectedIndex="0" Height="Auto" x:Name="TasksList" ItemsSource="{Binding ProgrammingTasks}" ItemTemplate="{StaticResource TaskListItemTemplate}" Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" SelectionChanged="TasksList_SelectionChanged">
        </ListBox>
        <TextBlock Text="{Binding ElementName=TasksList, Path=SelectedItem.Description}" Grid.Column="0" Grid.Row="1"></TextBlock>
        <ListBox SelectedIndex="0"  ItemsSource="{Binding ProgrammingLanguages}" Grid.Column="1" Grid.Row="2"  SelectionChanged="ProgLangList_SelectionChanged"></ListBox>
        <TextBlock Text="{Binding Path=TaskStatus, Mode=TwoWay}" Grid.Column="1" Grid.Row="3"></TextBlock>
        <TextBox Text="{Binding Path=UserCode}" Grid.Column="0" Grid.Row="2" Grid.RowSpan="2" VerticalScrollBarVisibility="Visible"
     AcceptsReturn="True"></TextBox>
        <Button Click="Button_Click" Grid.Column="0" Grid.Row="4" Grid.ColumnSpan="2">Проверить</Button>
    </Grid>
</Window>
